# Command Patterns (Learned)

<!-- IMPROVEMENT-PLAN-LOOP: promoted from loop learnings. Edit freely and commit with the project. -->

- **Resiliency Debug Test Sweep** (2026-04-14): When changing council debug metadata or degraded-run behavior, always run `pytest -q tests/test_resiliency_debug.py tests/test_multi_turn.py` because it catches both new debug-path regressions and the stage-helper signature drift that can break multi-turn flows. [workflow: improvement-plan-loop]

- **Council Metrics Regression Sweep** (2026-04-15): When changing council metrics, degraded-run metadata, or stream-path telemetry, always run `pytest -q tests/test_council_metrics.py tests/test_resiliency_debug.py tests/test_multi_turn.py` because it catches collector drift, degraded-run regressions, and stream-path divergence together. [workflow: improvement-plan-loop]

- **Council Metadata Regression Sweep** (2026-04-15): When changing degraded-run UI state, assistant-message persistence, or council debug metadata, always run `pytest -q tests/test_resiliency_debug.py tests/test_council_metrics.py tests/test_multi_turn.py` because it catches direct-path, stream-path, and storage drift together. [workflow: improvement-plan-loop]

- **Chairman Heterogeneity Config Check** (2026-05-01): When changing council or chairman model configuration, always run `pytest -q tests/test_chairman_heterogeneity.py` because startup now fails on exact or same-family chairman/council overlap. [workflow: improvement-plan-loop]

- **Stage 1 Fan-out Regression Sweep** (2026-05-01): When changing provider fan-out, retry policy, or Stage 1 degraded-run behavior, always run `pytest -q tests/test_stage1_backoff.py tests/test_resiliency_debug.py tests/test_council_metrics.py tests/test_multi_turn.py` because it covers limiter scheduling plus the existing debug, metrics, and multi-turn contracts. [workflow: improvement-plan-loop]

- **MCP Progress Regression Sweep** (2026-05-17): When changing council stage progress, MCP liveness, or FastMCP context wiring, always run `pytest -q tests/test_progress_callback.py` because it covers orchestrator progress order, heartbeat behavior, callback failure isolation, and tool-schema safety. [workflow: improvement-plan-loop]

- **Council Confidence Regression Sweep** (2026-05-17): When changing ranking parsing, chairman confidence prompts, or persisted assistant metadata, always run `pytest -q tests/test_council_confidence.py tests/test_council_metrics.py` because it covers the pure confidence signal plus direct/stream persistence drift. [workflow: improvement-plan-loop]

- **Frontend Stream Progress Sweep** (2026-05-18): When changing browser stream progress, run `pytest -q tests/test_council_metrics.py tests/test_multi_turn.py`, `npm run lint`, and a production `npm run build -- --outDir /var/folders/3j/zmvdy5_56bjcg1kmrx05dltcf7yldq/T/opencode/llm-council-frontend-progress-build --emptyOutDir` because the contract spans backend stream metadata, Stage 0 follow-up metadata, and React rendering. [workflow: improvement-plan-loop]
- **Chairman Attribution Regression Sweep** (2026-05-18): When changing chairman prompts, Stage 3 attribution metadata, or frontend/MCP trust rendering, always run `pytest -q tests/test_chairman_attribution.py tests/test_council_confidence.py tests/test_council_metrics.py tests/test_multi_turn.py` because the contract spans prompt generation, validator output, run-status persistence, and caller-facing rendering. [workflow: improvement-plan-loop]
- **MCP Cancellation Regression Sweep** (2026-05-18): When changing MCP cancellation, heartbeat cleanup, or provider fan-out, always run `pytest -q tests/test_progress_callback.py` because it now covers parent cancellation, child provider task cancellation, request-context reset, and no partial assistant storage. [workflow: improvement-plan-loop]

- **Clarification Gate Regression Sweep** (2026-05-18): When changing first-turn ambiguity routing or clarification persistence, run `pytest -q tests/test_clarification_gate.py tests/test_multi_turn.py tests/test_progress_callback.py` because the contract spans classifier parsing, storage metadata, follow-up context boundaries, and MCP progress/schema behavior. [workflow: improvement-plan-loop]

- **Deliberation Mode Regression Sweep** (2026-05-18): When changing quick/standard/deep routing, run `pytest -q tests/test_deliberation_mode.py tests/test_progress_callback.py tests/test_council_metrics.py tests/test_clarification_gate.py` because the contract spans classifier fallback, direct and streaming execution, MCP progress/schema behavior, metrics, and clarification short-circuits. [workflow: improvement-plan-loop]
- **Semantic Answer Cache Regression Sweep** (2026-05-18): When changing semantic cache matching, validation, or cache metadata, run `pytest -q tests/test_answer_cache.py tests/test_deliberation_mode.py tests/test_multi_turn.py tests/test_council_metrics.py` because the contract spans direct API, stream API, MCP schema, storage metadata, mode eligibility, and metrics boundaries. [workflow: improvement-plan-loop]
- **Confidence Escalation Regression Sweep** (2026-05-18): When changing post-Stage-2 routing, always run `pytest -q tests/test_council_confidence.py tests/test_council_metrics.py` because direct orchestration and the streaming endpoint can diverge. [workflow: improvement-plan-loop]

- **Answer Cache KPI Regression Sweep** (2026-05-18): When changing answer-cache matching, thresholds, validation, or metrics, run `pytest -q tests/test_answer_cache.py tests/test_council_metrics.py` and `python scripts/answer_cache_replay.py --limit 200 --samples 10` because the contract spans runtime counters, metrics exposure, and offline quality samples. [workflow: improvement-plan-loop]

- **Judge Evaluation Regression Sweep** (2026-05-28): When changing the judge rubric, parser, baseline selection, or generation options, run `pytest -q tests/test_judge_eval.py` and `python scripts/judge_eval_smoke.py --output /tmp/llm-council-judge-eval-smoke.json` because the contract spans strict JSON parsing, provider-call options, and the operator artifact shape. [workflow: improvement-plan-loop]

- **Adaptive Routing Regression Sweep** (2026-05-29): When changing agent routing, Stage 1/2 model selection, route metadata, or expansion policy, run `pytest -q tests/test_agent_routing.py tests/test_deliberation_mode.py tests/test_council_confidence.py tests/test_council_metrics.py tests/test_stage1_backoff.py tests/test_progress_callback.py tests/test_multi_turn.py tests/test_judge_eval.py` and `python scripts/agent_routing_benchmark.py --output /tmp/llm-council-agent-routing-benchmark.json` because the contract spans direct orchestration, streaming, metrics, cancellation, multi-turn, and judge-smoke quality gates. [workflow: improvement-plan-loop]

- **Stage 2b Revision Policy Sweep** (2026-06-03): When changing deep-mode revision prompts, confidence escalation into 2a/2b, or Stage 3 synthesis of revised responses, run `pytest -q tests/test_stage2b_revision_policy.py tests/test_council_confidence.py tests/test_council_metrics.py tests/test_progress_callback.py` because the contract spans evidence-gated revision prompts, direct orchestration, FastAPI streaming, and cancellation cleanup. [workflow: improvement-plan-loop]
